const serviceTypes = {
    ra: "rechargeAccumulate",   // 声望累积
    vip: "vip", //普通vip
}

const costCurrency = {
    "yuan": "yuan",
    "reputation": "reputation",
}

module.exports = {
    serviceTypes,
    costCurrency,
}